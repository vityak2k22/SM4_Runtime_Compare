#include "Config.h"

//=========================================================================================================
// Внесення всього тексту у рядок (враховуючи переноси рядка)
extern string Input_from_File(ifstream& in) {
    string str;
    char c;
    while (in.get(c))
        str += c;
    return str;
}
//=========================================================================================================